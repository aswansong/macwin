# 当前执行计划

计划 ID：`Alpha-0.1-vertical-slice`

状态：**Alpha 0.1 真实纵向切片执行中**（`alpha_local_validation_active`）

最后更新：2026-08-02

负责人角色：产品负责人、技术负责人、反方评审（Codex）；最终拍板人：仓库负责人

## 0. 当前授权与执行门

负责人已接受 M1 原型并授权在独立分支 `alpha/v0.1-vertical-slice` 完成最小真实纵向切片。[D-030、E-019]

本轮允许：Windows 10/11 x64 白名单扫描、现有 `.habitpack` 1.0.0 生成与导入、强制计划、macOS Finder 扩展名与键盘重复速率的声明式应用、单份迁移前快照、按模块恢复、报告和指南。实现必须默认本地离线、无账号、零上传、只执行代码内固定规则。

本轮明确不做：Wi‑Fi/密码、Ctrl/Command 兼容层、第三方工具或 Homebrew 安装、软件自动安装、UAC/TCC/管理员流程、真实秘密、个人文件、更新、签名/公证、公开 Release。浏览器与软件只做白名单检测及官方入口提示。[D-030、E-019]

当前分支不得合并 `main` 或创建 Release；可提交并推送 Alpha 分支供明确知情测试者使用。主控可见可用额度低于 20% 时停止新任务并保留目标。

## 1. 阶段背景

`M0-documentation-baseline` 已经完成并发布到 `main`。M0 交付了产品简报、证据、决策、规格、机器可读功能清单、仓库规则和安全政策；它是准备阶段，不替代负责人已经确认的首个产品验证里程碑。[E-015、E-016]

M1“完整可点击原型＋迁移格式规范”已经完成并停止。第二版原型只回答虚构浏览器交互问题，格式资产只验证 schema、容器和虚构夹具；两者都不证明真实系统功能已经实现。[D-024、D-028]

## 2. M1 历史授权（已关闭）

负责人已经授权：

- 以独立分支 `prototype/ui-flow-v2-hardening` 的冻结提交归档完整可点击流程；原型运行时不访问外部网络，本机开发服务器不视为产品联网能力；
- 原型使用虚构扫描结果、迁移包、权限结果、失败和恢复状态；
- 在 `main` 完善 `.habitpack` 目录、manifest、模块、秘密引用、完整性、兼容性和拒绝规则；
- 为格式提供 JSON Schema、有效/无效虚构夹具和不接触真实系统的结构校验；
- 使用 `prototype` 方法验证流程，使用 `frontend-design` 约束视觉，并记录原型结论。[D-021、D-024、D-027]

以下是 M1 当时尚未授权的事项；其中部分真实能力后来由 D-030 以更窄边界授权，未列入 Alpha 的能力仍保持未授权：

- 创建生产 Tauri 或其他正式应用骨架；
- 读取当前电脑或测试者电脑的真实注册表、偏好、Wi‑Fi 凭据或软件清单；
- 请求 UAC、TCC、管理员权限或修改任何系统设置；
- 实现真实扫描、安装、键盘映射、网络写入、快照或恢复；
- 下载、安装或引入 Karabiner-Elements、LinearMouse、Homebrew 等生产依赖；
- 生成安装包、创建 Release 或分发 Alpha；
- 从私人历史目录复制真实配置、日志或个人数据到仓库。[D-023、D-024、D-025]

## 3. M2 Wave 0 本轮授权（历史记录）

当时负责人明确允许先在 `main` 更新 M2 授权与执行计划文档并提交推送；允许在 `codex/m2-*` 独立研究分支编写、测试、提交并推送研究代码、虚构夹具和报告，但禁止 PR、合并 `main`、Release。[D-029、E-018]

允许的四个研究流及范围：

1. **`.habitpack` 1.0.0 独立 parser**：只处理 M1 已定义的包、schema、完整性和拒绝夹具；入口是本地虚构包，停止条件是真实迁移包、真实秘密或新增格式边界，完成定义是虚构有效/无效夹具与报告可重复通过。
2. **一次性 Tauri 2 最小壳与 mock bridge**：只验证壳与声明式 mock 接口的边界，不接真实平台桥；入口是本地 mock，停止条件是需要读取/写入系统或生产骨架，完成定义是隔离构建、mock 调用和边界测试通过。
3. **模拟快照/恢复**：只使用 synthetic snapshot 记录和虚构状态，验证快照先于模拟写入、按模块恢复和秘密不进入快照；入口是本地虚构状态，停止条件是真实偏好、权限或设备，完成定义是失败、恢复和秘密扫描夹具可重复通过。
4. **离线签名与供应链虚构测试**：只验证离线签名材料、锁定版本元数据和 fictional supply-chain fixture 的拒绝/回滚行为；入口是虚构签名与来源，停止条件是生产密钥、第三方实际安装或真实下载，完成定义是签名错误、撤销、回滚和来源不匹配夹具可重复通过。

Wave 0 还允许通过受限 HTTPS 阅读官方文档，并在项目或临时隔离环境安装锁定版本的开发验证依赖；不得上传迁移包、设备信息、用户数据或秘密。[D-029、E-018]

Wave 0 当时不授权读取或修改真实系统设置、真实设备验证、UAC/TCC/管理员权限、Wi‑Fi 密码、Karabiner/LinearMouse/Homebrew/系统服务/应用包安装、生产依赖、生产骨架、Alpha 或 Release；当时 15 个 P0 均为 `specified`。该段是历史边界，不覆盖当前 D-030 的 Alpha 授权。[D-029、E-018]

主控可见可用额度低于 20% 时，停止派发新任务并保留目标；这是执行门禁，不是产品功能状态，不得用 `goal complete` 或 `blocked` 伪装暂停。[D-029、E-018]

## 4. M1 交付物（历史）

| 交付物 | 验收方式 | 当前状态 |
|---|---|---|
| 完整可点击原型 | Windows 导出与 Mac 导入两端主流程均可点击；覆盖成功、权限拒绝、模块失败、人工处理和恢复 | 已冻结并接受 |
| 原型隔离 | 只在冻结原型分支；虚构数据；不联网、不提权、不读取或修改系统 | 已完成 |
| 交互验证 | 紧凑三阶段流程、强制计划预览、权限解释、动作级状态、报告、指南和恢复联动 | 已完成 |
| 视觉验证 | 中文优先；明亮友好、有探索感；满足可访问性和禁止视觉清单 | 已完成 |
| `.habitpack` 规范 | 文件集合、schema、哈希关系、秘密引用、版本与拒绝行为不存在循环或双重权威 | 已完成（仅 M1 格式范围） |
| 格式资产 | JSON Schema 与有效/无效虚构夹具可由单一命令校验 | 已完成（虚构夹具） |
| 原型结论 | 记录保留、修改、否定的交互假设和进入正式实现前的决定点 | 已归档 |

## 5. M1 执行顺序（历史）

1. **校准授权记录与格式歧义** — 已完成；修正 M0/M1 状态、完整性关系和文档校验规则。
2. **冻结 M1 格式表面** — 已完成；manifest、模块 JSON Schema、允许路径、固定上限和虚构夹具已闭合。
3. **创建原型分支与脚手架** — 已完成并冻结在独立分支。
4. **实现端到端可点击流程** — 已完成虚构浏览器交互验证。
5. **加入关键分支** — 已完成虚构拒绝、失败、重试和恢复状态验证。
6. **视觉与可访问性复查** — 已完成原型范围复查。
7. **格式与文档校验** — 已由 `./scripts/validate-m1` 通过。
8. **记录原型结论并停止** — 已完成；M1 已关闭，Wave 0 按本文件单独执行。

## 6. M1 完成定义（历史）

M1 只有在以下条件全部满足后才能标为完成：

- 原型分支可用一条文档化命令启动，且显著标记“虚构数据，不会修改系统”；
- Windows → `.habitpack` → Mac 的主流程和关键失败/恢复分支均可点击；
- 用户不能绕过迁移计划直接进入模拟执行；
- Wi‑Fi 密码、管理员权限、Homebrew 和第三方工具默认不勾选且首层标识风险；
- 内置键盘 Ctrl 兼容、检测到的外接 Windows 键盘兼容和个性化指南按决定预选；
- 原型没有对外网络请求、外部 API、原生桥接、Shell/PowerShell、系统读取、系统写入或真实凭据；本机开发服务器仅用于展示原型；
- `.habitpack` 的所有非 manifest 文件被唯一声明并校验，秘密路径和普通路径不存在歧义；
- 有效夹具通过，路径穿越、未声明文件、碰撞、超限、哈希错误和未知规则夹具被拒绝；
- 原型结论明确回答问卷负担、计划可理解性、风险提示、错误恢复和中文信息层级；
- 公开文档不把原型或格式夹具描述为已实现的迁移产品。

## 7. M1 完成证据与人类决策边界（历史）

- 原型：提交 `37edc4edd52f2d0fb5aa7d796faa5fd7437bbb75`；范围仅为 `fictional_browser_interaction_only`。
- 格式：`schema_version` 精确为 `1.0.0`；范围仅为 `schema_container_and_fictional_fixture_validation_only`；统一命令为 `./scripts/validate-m1`。
- 所有真实 P0 功能（包括 P0-004）继续保持 `specified`，没有实现或真实设备证据。

M1 已在不关闭 OD-001 至 OD-011 的情况下完成，但不得把未决值伪装成正式承诺：

- OD-001 支持入口在原型中可以使用明确占位符；公开发布前必须替换；
- OD-004 色值、Logo 和动效参数可做原型候选，不视为品牌定稿；
- OD-005 未关闭前，Wi‑Fi 凭据只能使用不可逆虚构占位数据，不得实现平台读取或写入；
- OD-006、OD-007 未关闭前，不实现真实更新或第三方下载；
- OD-009、OD-010、OD-011 可在原型和格式样例中比较选项，但最终默认值仍由负责人拍板。

OD-003、OD-005、OD-006、OD-007、OD-011 不阻塞 M1 格式收口，但继续阻塞相关生产实现；OD-001、OD-002、OD-004、OD-008、OD-009、OD-010 仍未解决。

任何新增迁移数据类别、改变默认勾选、取消强制计划预览、放宽秘密边界或开始真实系统修改的提议都必须再次由负责人确认。[AGENTS.md]

## 8. 执行 agent 可自主决定的细节（M1 历史）

以下是 M1 执行期间的自主边界；M1 关闭后不得据此继续 M2 或生产实现。在不改变已决定行为和安全边界时，执行 agent 可以自主决定：

- 原型内部组件拆分、路由、状态夹具、测试框架、开发端口和格式化；
- 不构成品牌定稿的候选色值、字体回退、图标和轻量动效；
- JSON Schema 文件拆分、测试夹具目录、错误枚举和验证脚本组织；
- 固定安全上限的初始保守建议值，但必须记录并可测试；
- 同一路由中的原型方案切换方式和虚构结果组合。

## 9. M2 Wave 0 完成定义与后续决策点

Wave 0 只有在四个研究流各自满足入口、停止和完成定义，且下列条件全部满足后才能标为完成：

- 所有研究内容位于 `codex/m2-*` 分支，不创建 PR、不合并 `main`、不创建 Release；
- 只使用本地虚构夹具、mock、synthetic snapshot、离线 signature 和 fictional supply-chain 数据；
- 研究报告明确区分已观察结果、待验证假设和未授权的真实平台结论；
- `./scripts/validate-m1` 继续证明 M1 资产完整、Alpha 状态与 `feature-list.json` 一致；
- 没有真实设备、真实秘密、权限调用、系统写入、第三方实际安装、生产依赖、生产骨架、生产签名密钥或产品边界变化。

Wave 0 完成后，负责人仍需决定哪些研究结论进入真实平台 R2、哪些 M1 交互结论进入正式产品，以及是否授权生产骨架、Alpha 或公开发布。[D-022、D-023、D-029、E-018]

### M1 之后的历史决策点（已由本轮授权更新）

M1 已按默认规则停止。负责人需要决定：

1. 原型中的哪些交互结论进入正式产品；
2. M2 Wave 0 已按本文件局部授权；真实平台 R2 的平台设置、权限、Wi‑Fi、生产快照、生产更新签名和第三方实际供应链仍待负责人拍板；
3. 哪些 OD 项必须在生产骨架前关闭；
4. 是否继续采用 Tauri 2，或根据 M2 证据更换技术路线。[D-022]

该结论属于 Wave 0 结束时的历史门槛；当前 Alpha 只在本分支按 D-030 允许的最小范围实现，仍不得在 `main` 创建生产 Release。[D-029、D-030、E-018、E-019]
